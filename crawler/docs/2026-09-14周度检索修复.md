# 2026-09-14 周度检索修复

本次仍使用 V1.5 词库，用户任务包与已导入配置的 SHA256 一致。网页上的项目数、正文数和金额以本次生成的报告为准，不复用上期固定数字。

## 原式分支轮转

原计划在每个分支下依次遍历 135 个来源，192 条预算会主要消耗在 A、B 的第一组。已为执行器加入跨来源、A—F 分支交错排序和跨周游标。原式文本、原始字段要求、时间范围和查询身份均保留；两项回归用例验证有限预算包含全部分支、全部来源，以及断点接续不丢失查询。

## 搜索结果来源校验

对北京政府采购网的限定检索实测返回百度百科、壹心理等无关域名；旧的相关性检查只看中文心理词，因此仍记作搜索响应成功。本次增加域名校验，不满足 site 限定的结果进入失败及备用引擎流程。两项回归用例验证拒绝无关域名并保留正确来源。

本轮原 192 条记录没有产生满足域名及原式的候选，已保留 original_status 并补充复核状态 query_constraints_unverified。后续 3 条复查使用修复后的校验。不能将这些响应当作源站无信息或站内分页完成。

## 江西 HTTPS 与浏览器路径

Python 通过环境代理和直连都出现 UNSAFE_LEGACY_RENEGOTIATION_DISABLED；普通 Chrome 能加载江西官网，同一公开检索接口返回 200，心理宽种子第一页报告 290 条。

新增 scripts/epoint_browser_search.cjs，从正常加载的官网页面请求公开 JSON 检索，保留页面原始 JSON、哈希、检索字段、年度范围、总数、重复页及断点。没有关闭证书验证、启用不安全 TLS 或进行验证绕过。

本轮江西共执行 42 组宽种子，取得 75 页、365 个唯一结果 URL；结果还需按 A—F 原式全文筛选。陕西、重庆继续使用其各自已验证的公开检索路径。

## 统计与验证边界

- 入口探测、站外检索、站内结果、正文和独立项目数分别统计。
- 省级入口普通浏览器检查与原有站内查询记录分别保留；首页加载不能证明检索可用。
- 剑鱼只采纳公开可见列表；未核实标题和详情链接对应关系的行不直接发布为项目。
- 云端失败保留本次记录，不循环请求同一批受阻链接。
- 报告同时展示当前自然周和最近完整周，标明各自日期。
- 回归测试共 155 项通过；发布前另核对实际数据、正文加载与线上版本。

另修复了核心育种跨词误命中心育的问题。该农业词组本身不触发心理采购匹配，含真实心理健康服务的同类单位采购仍保留。

补充教育体育局、教育科技体育局、教体局、教师发展中心、职业教育中心及义务教育场景识别，并重新计算受影响公告的教育分类；卫生健康部门的同类项目仍区分为其他心理。

导出前重新检查全文证据：验证码、登录或付费提示、只有页脚的空壳页面，以及只有扫描水印或页码的 PDF 不计作有效全文，并在详情保留验证或 OCR 缺口。短小但含真实采购参数的文件继续保留。

千里马样本使用普通 Chrome 实际点击验证码并尝试验证，页面仍提示验证失败；刷新后验证码图像未完成加载。继续保留验证缺口，未算作正文取得。

补充“中标（成交）金额”及英文括号变体的段落和表格金额提取，并重新处理受影响公告，避免有正文却缺失成交金额。报告另按上期待补清单追踪补齐、仍待补和新增待补数量，避免将扩大发现范围后的待补总数误解为补抓结果。

证据重解析加入自动接续：单轮时间上限后，下一批优先处理尚未解析的已下载公告；最多四批，剩余队列写入 pending-seeds.json，并在下一次运行优先接续。已用本轮实际 169 条尾部记录验证接续覆盖。

中标单位复核补充：评审报价/排序表中的其他投标人不作为中标单位；优先读取明确的中标供应商字段，并支持“采购结果公告”中的正式成交表。
